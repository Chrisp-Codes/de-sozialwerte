name: Validate JSON

on:
  pull_request:
    paths:
      - 'data/**'
      - 'schema/**'
  push:
    paths:
      - 'data/**'
      - 'schema/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install ajv-cli
        run: npm install -g ajv-cli ajv-formats

      - name: Validate all data files against schema
        run: |
          count=0
          for file in data/*.json; do
            echo "Validating $file..."
            ajv validate \
              --spec=draft7 \
              -c ajv-formats \
              -s schema/values.schema.json \
              -d "$file"
            count=$((count+1))
          done

          if [ "$count" -eq 0 ]; then
            echo "::error::No data files found."
            exit 1
          fi

          echo "✓ Validated $count files."

      - name: Check that latest.json matches current year
        run: |
          YEAR=$(date +%Y)
          LATEST_YEAR=$(jq -r '.year' data/latest.json)
          echo "Current year: $YEAR, latest.json year: $LATEST_YEAR"
          if [ "$LATEST_YEAR" -lt "$YEAR" ]; then
            echo "::warning::latest.json references year $LATEST_YEAR, but current year is $YEAR. Please update latest.json."
          fi
